//use reveldb::{FileLogWriter, Record, RecordType};

fn main() {
    /*
    let mut db = FileLogWriter::new("dbs/demo/".to_owned());
    let record = Record::new(&[1], RecordType::FULL);
    record.encode_to_file(&mut db)
    */
}
